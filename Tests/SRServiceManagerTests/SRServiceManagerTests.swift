import XCTest
@testable import SRServiceManager

final class SRServiceManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SRServiceManager().text, "Hello, World!")
    }
}
